{
  "name": "creative-boilerplate",
  "version": "0.2.0",
  "scripts": {
    "server": "NODE_ENV=developement webpack-dev-server",
    "dev": "NODE_ENV=developement webpack-dev-server",
    "start": "node startup.js",
    "build": "cp ./src/*.html ./build/ & cp -R ./src/assets ./build/ & webpack -p"
  },
  "dependencies": {
    "csv": "^2.0.0"
  },
  "paths": {},
  "devDependencies": {
  }
}
